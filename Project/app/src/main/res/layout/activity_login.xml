<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">

    <ImageView
        android:id="@+id/chat_synergykit"
        android:layout_height="200dp"
        android:layout_width="200dp"
        android:src="@drawable/ic_firebase"
        android:contentDescription="@string/login_chat_synergykit" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_marginTop="@dimen/loginActivityTextViewMarginTop"
        android:layout_marginBottom="@dimen/loginActivityTextViewMarginBottom"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="vs" />

    <ImageView
        android:id="@+id/chat_firebase"
        android:layout_height="200dp"
        android:layout_width="200dp"
        android:src="@drawable/ic_firebase"
        android:contentDescription="@string/login_chat_synergykit" />

</LinearLayout>
